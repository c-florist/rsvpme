---
export interface Props {
  size?: string;
  class?: string;
}

const { size = "w-5 h-5", class: className = "" } = Astro.props;
---

<svg
  class={`${size} ${className}`}
  fill="none"
  stroke="currentColor"
  viewBox="0 0 24 24"
>
  <slot />
</svg>
